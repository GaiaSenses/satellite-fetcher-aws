# Congelado por completo (pip freeze dentro da imagem base pinada) para o build
# ser reproduzível. Dependências diretas: numpy, pandas, shapely, netCDF4,
# boto3 — geopandas saiu (era um import sem nenhum uso, e arrastava GDAL,
# pyproj e pyogrio para dentro da imagem). Para atualizar: suba as diretas,
# rode o freeze dentro da imagem e regrave este arquivo inteiro.

# — diretas —
boto3==1.42.97
netCDF4==1.7.4
numpy==2.5.3
pandas==3.0.5
shapely==2.1.2

# — transitivas (arrasto das diretas) —
awslambdaric==4.0.2
botocore==1.42.97
certifi==2026.7.22
cftime==1.6.5
jmespath==1.0.1
python-dateutil==2.9.0.post0
s3transfer==0.16.0
simplejson==4.1.2
six==1.17.0
snapshot-restore-py==1.0.0
urllib3==2.7.0
